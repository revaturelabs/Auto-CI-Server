apiVersion: v1 # for versions before 1.9.0 use apps/v1beta2
kind: Service
metadata:
  namespace: project3
  name: nginx-service
spec:
  type: NodePort
  selector:
    app: nginx
  ports:
    - name: sample
      port: 80
      targetPort: 80
      nodePort: 30000

  